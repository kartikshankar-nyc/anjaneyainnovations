---
import MainLayout from '../layouts/MainLayout.astro';
import { getCollection } from 'astro:content';

// Fetch values, sorted by order
const values = (await getCollection('values')).sort(
  (a, b) => (a.data.order ?? 99) - (b.data.order ?? 99)
);

// Fetch team members, sorted by order
const teamMembers = (await getCollection('team')).sort(
  (a, b) => (a.data.order ?? 99) - (b.data.order ?? 99)
);
---

<MainLayout title="About Us - Anjaneya Innovations">
  <div class="page-wrapper">
    <section class="page-header section-spacing">
      <div class="container">
        <h1 class="section-heading">About Anjaneya Innovations</h1>
        <p class="header-tagline">Defining the next generation of technology partnership.</p>
      </div>
    </section>

    <section class="company-story section-spacing animate-on-scroll">
      <div class="container">
        <div class="section-content">
          <h2 class="section-heading">Our Story</h2>
          <p class="section-text">Founded with the vision of transforming how businesses leverage technology, Anjaneya Innovations emerged from the recognition that traditional technology consulting needed reinvention. Named after the Hindu deity symbolizing strength, intelligence, and loyal service, we embody these qualities in all our client engagements.</p>
          
          <p class="section-text">Our journey began when a team of industry veterans, frustrated by the limitations of conventional consulting models, came together to create a more integrated, outcome-focused approach. We've since evolved into a cutting-edge technology partner for forward-thinking organizations seeking transformative results in an AI-driven world.</p>
          
          <div class="story-highlight">
            <p>Today, Anjaneya Innovations stands at the intersection of strategic consulting, advanced AI implementation, and elite software engineering—offering a unique combination that drives measurable business impact for our clients.</p>
          </div>
        </div>
      </div>
    </section>

    <section class="about-intro section-spacing animate-on-scroll">
      <div class="container">
        <div class="about-grid">
          <div class="about-content">
            <h2 class="section-heading">Our Approach</h2>
            <p class="section-text">Anjaneya Innovations was conceived to redefine technology consulting. We operate as a deeply integrated partner, embedding strategic expertise, AI-first thinking, and elite engineering talent directly within our clients' organizations.</p>
            
            <p class="section-text">Our remote-first model ensures agility and global expertise, while radical transparency builds trust and alignment. We achieve exceptional, data-driven outcomes through a principled, client-centric approach.</p>
            
            <div class="approach-pillars">
              <div class="pillar">
                <h3>Strategic Integration</h3>
                <p>We work as an extension of your team, aligning our expertise with your business objectives.</p>
              </div>
              
              <div class="pillar">
                <h3>Technical Excellence</h3>
                <p>Our solutions combine cutting-edge technologies with proven engineering practices.</p>
              </div>
              
              <div class="pillar">
                <h3>Measurable Impact</h3>
                <p>We define success through tangible business outcomes and data-driven results.</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section class="mission-vision section-spacing animate-on-scroll">
      <div class="container">
        <div class="purpose-content">
          <h2 class="section-heading">Our Purpose</h2>
          <p class="section-text"><strong>Mission:</strong> To accelerate client success through transformative, AI-enhanced technology solutions—delivered with uncompromising quality, speed, and integrity.</p>
          <p class="section-text"><strong>Vision:</strong> To be the most trusted and impactful technology partner for businesses navigating critical change and pursuing ambitious goals in the AI era.</p>
        </div>
      </div>
    </section>

    <section class="values section-spacing animate-on-scroll">
      <div class="container">
        <h2 class="section-heading centered">Our Core Values</h2>
        <div class="values-grid">
          <div class="value-card">
            <h3>Integrity</h3>
            <p>Upholding the highest ethical standards. Radical honesty and transparency in every interaction with clients and partners.</p>
          </div>
          
          <div class="value-card">
            <h3>Excellence</h3>
            <p>A commitment to best-in-class engineering, strategic thinking, and delivering exceptional, measurable results.</p>
          </div>
          
          <div class="value-card">
            <h3>Partnership</h3>
            <p>Operating as true partners, fostering collaboration, shared ownership, and mutual success with clients and our internal team.</p>
          </div>
          
          <div class="value-card">
            <h3>Agility</h3>
            <p>Embracing a nimble, adaptive approach to quickly meet client needs and respond to evolving market dynamics.</p>
          </div>
        </div>
      </div>
    </section>

    <section class="team section-spacing animate-on-scroll">
      <div class="container">
        <h2 class="section-heading centered">Our Leadership Team</h2>
        <div class="team-grid">
          <div class="team-member">
            <div class="member-image">
              <div class="image-placeholder">
                <!-- Remove leftover SVG icon -->
              </div>
            </div>
            <h3>John Doe</h3>
            <p class="position">Founder & CEO</p>
            <p class="bio">With over 15 years of experience in business consulting, John leads our team with vision and expertise.</p>
          </div>
          
          <div class="team-member">
            <div class="member-image">
              <div class="image-placeholder">
                <!-- Remove leftover SVG icon -->
              </div>
            </div>
            <h3>Jane Smith</h3>
            <p class="position">Chief Strategy Officer</p>
            <p class="bio">Jane brings extensive knowledge in strategic planning and business development to our clients.</p>
          </div>
          
          <div class="team-member">
            <div class="member-image">
              <div class="image-placeholder">
                <!-- Remove leftover SVG icon -->
              </div>
            </div>
            <h3>Michael Johnson</h3>
            <p class="position">Technology Director</p>
            <p class="bio">Michael's expertise in technology solutions helps businesses implement innovative digital strategies.</p>
          </div>
        </div>
      </div>
    </section>

    <section class="cta-section section-spacing animate-on-scroll">
      <div class="container">
        <div class="section-content text-center">
          <h2 class="section-heading">Let's Build the Future Together.</h2>
          <p class="section-text">Discover how a partnership with Anjaneya Innovations can accelerate your transformation.</p>
          <a href="/contact" class="btn btn-primary">Get in Touch</a>
        </div>
      </div>
    </section>
  </div>
</MainLayout>

<style>
  /* Page Wrapper */
  .page-wrapper {
    width: 100%;
    overflow-x: hidden;
  }
  
  /* Common Section Styling */
  .section-spacing {
    padding: var(--space-3xl) 0;
    border-bottom: 1px solid var(--border-color);
  }
  
  .section-spacing:last-child {
    border-bottom: none;
  }
  
  /* Container */
  .container {
    max-width: var(--max-width);
    width: 100%;
    margin: 0 auto;
    padding: 0 var(--space-xl);
  }
  
  /* Section Content */
  .section-content {
    width: 100%;
    max-width: 900px;
    margin: 0 auto;
  }
  
  /* Text Styles */
  .section-text {
    font-size: 1.1rem;
    line-height: 1.8;
    color: var(--secondary-color);
    margin-bottom: var(--space-lg);
  }
  
  .text-center {
    text-align: center;
  }

  /* Restyle page header */
  .page-header {
    color: var(--primary-color);
    text-align: center;
    position: relative;
    overflow: hidden;
  }
  
  .page-header::before, 
  .page-header::after {
     display: none; 
  }

  .section-heading {
    font-size: clamp(2.2rem, 5vw, 3rem);
    font-weight: 800;
    color: var(--primary-color);
    margin-bottom: var(--space-lg);
    position: relative;
    display: inline-block;
    padding-bottom: var(--space-xs);
    border-bottom: 2px solid var(--accent-color);
  }
  
  h1.section-heading {
    font-size: clamp(2.8rem, 5.5vw, 4rem);
  }
  
  .section-heading.centered {
    text-align: center;
    margin-left: auto;
    margin-right: auto;
  }

  .header-tagline {
    font-size: 1.2rem;
    max-width: 700px;
    margin: 0 auto;
    font-weight: 400;
    color: var(--secondary-color);
  }
  
  /* Company Story Section */
  .company-story {
    background-color: var(--background-color);
  }
  
  .story-highlight {
    background-color: var(--card-bg-color);
    border-left: 3px solid var(--accent-color);
    padding: var(--space-lg);
    margin: var(--space-xl) 0;
    border-radius: 0 var(--radius-default) var(--radius-default) 0;
  }
  
  .story-highlight p {
    font-size: 1.15rem;
    line-height: 1.7;
    color: var(--primary-color);
    margin: 0;
    font-weight: 500;
  }

  /* About Approach Section */
  .about-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 0;
    align-items: center;
  }

  .approach-pillars {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--space-lg);
    margin: var(--space-xl) 0;
  }
  
  .pillar {
    background-color: var(--card-bg-color);
    padding: var(--space-lg);
    border-radius: var(--radius-default);
    border: 1px solid var(--border-color);
    transition: transform 0.3s ease;
  }
  
  .pillar:hover {
    transform: translateY(-5px);
    border-color: var(--accent-color);
  }
  
  .pillar h3 {
    font-size: 1.2rem;
    color: var(--primary-color);
    margin-bottom: var(--space-sm);
    font-weight: 700;
  }
  
  .pillar p {
    font-size: 0.95rem;
    color: var(--secondary-color);
    margin: 0;
    line-height: 1.6;
  }

  /* Mission Vision */
  .mission-vision {
    background-color: var(--background-color);
  }

  .purpose-content p strong {
    color: var(--primary-color);
    font-weight: 700;
  }

  /* Values Section */
  .values-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--space-lg);
    margin-top: var(--space-xl);
  }

  .value-card {
    background-color: var(--card-bg-color);
    padding: var(--space-xl);
    border-radius: var(--radius-default);
    border: 1px solid var(--border-color);
    transition: transform 0.3s ease, border-color 0.3s ease;
    height: 100%;
  }

  .value-card:hover {
    transform: translateY(-5px);
    border-color: var(--accent-color);
  }

  .value-card h3 {
    font-size: 1.3rem;
    margin-bottom: var(--space-sm);
    color: var(--primary-color);
    font-weight: 700;
  }
  
  .value-card p {
    font-size: 0.95rem;
    color: var(--secondary-color);
    line-height: 1.6;
    margin: 0;
  }

  /* Team Section */
  .team-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--space-xl);
    margin-top: var(--space-xl);
  }

  .team-member {
    text-align: center;
    background-color: var(--card-bg-color);
    padding: var(--space-xl);
    border-radius: var(--radius-default);
    border: 1px solid var(--border-color);
    transition: transform 0.3s ease, border-color 0.3s ease;
  }
  
  .team-member:hover {
    transform: translateY(-5px);
    border-color: var(--accent-color);
  }

  .member-image {
    margin-bottom: var(--space-lg);
    width: 120px; 
    height: 120px;
    margin-left: auto;
    margin-right: auto;
    border-radius: 50%; 
    overflow: hidden; 
    border: 1px solid var(--border-color);
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: var(--background-color);
  }

  .member-image .image-placeholder {
    padding: 0; 
    border-radius: 50%; 
    width: 100%;
    height: 100%;
    background: radial-gradient(circle, var(--card-bg-color) 0%, var(--background-color) 100%);
    position: relative;
    overflow: hidden;
  }
  
  .member-image .image-placeholder::before {
    content: '';
    position: absolute;
    top: var(--space-sm);
    left: var(--space-sm);
    right: var(--space-sm);
    bottom: var(--space-sm);
    border: 2px solid var(--accent-color);
    border-radius: 50%;
    opacity: 0.4;
  }

  .team-member h3 {
    font-size: 1.4rem;
    margin-bottom: var(--space-xs);
    color: var(--primary-color);
    font-weight: 700;
  }

  .team-member .position {
    color: var(--accent-color);
    font-weight: 600;
    margin-bottom: var(--space-sm);
    font-size: 0.9rem;
    display: block;
  }

  .team-member .bio {
    line-height: 1.6;
    font-size: 0.9rem;
    color: var(--secondary-color);
    margin: 0;
  }

  /* CTA Section */
  .cta-section {
    text-align: center;
  }
  
  .cta-section .section-text {
    max-width: 600px;
    margin: 0 auto var(--space-xl);
  }

  /* Button styles */
  .btn {
    display: inline-block;
    padding: 0.9rem 2.2rem; 
    border-radius: var(--radius-default); 
    text-decoration: none;
    font-family: var(--font-sans);
    font-weight: 800; 
    font-size: 0.9rem;
    transition: all 0.2s ease-in-out;
    border: 1px solid transparent;
    cursor: pointer;
    text-align: center;
    letter-spacing: 0.5px;
    text-transform: uppercase; 
  }

  .btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 3px 6px rgba(0,0,0,0.1); 
  }

  .btn-primary {
    background-color: var(--card-bg-color);
    color: var(--primary-color);
    border-color: var(--accent-color);
  }
  
  .btn-primary:hover {
    background-color: var(--accent-color);
    color: var(--text-on-light);
    border-color: var(--accent-color);
  }

  /* Responsive Styles */
  @media (max-width: 992px) {
    .approach-pillars {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 768px) {
    .section-spacing {
      padding: var(--space-2xl) 0;
    }
    
    .container {
      padding: 0 var(--space-md);
    }
    
    .section-heading {
      margin-bottom: var(--space-md);
    }
    
    .section-text {
      font-size: 1rem;
      margin-bottom: var(--space-md);
    }
    
    .story-highlight {
      padding: var(--space-md);
      margin: var(--space-lg) 0;
    }
    
    .story-highlight p {
      font-size: 1rem;
    }
    
    .team-grid,
    .values-grid {
      grid-template-columns: 1fr;
    }
  }
</style> 
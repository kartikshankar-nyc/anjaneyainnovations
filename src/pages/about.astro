---
import MainLayout from '../layouts/MainLayout.astro';
---

<MainLayout title="About Us - Anjaneya Innovations">
  <section class="page-header">
    <div class="container">
      <h1>About Anjaneya Innovations</h1>
      <p>Defining the next generation of technology partnership.</p>
    </div>
  </section>

  <section class="about-intro animate-on-scroll">
    <div class="container">
      <div class="about-grid">
        <div class="about-content">
          <h2>Our Approach</h2>
          <p>Anjaneya Innovations was conceived to redefine technology consulting. We operate as a deeply integrated partner, embedding strategic expertise and elite engineering talent directly within our clients' organizations.</p>
          <p>Our remote-first model ensures agility and access to global expertise, while our commitment to radical transparency builds trust and alignment. We believe in achieving exceptional outcomes through a principled, client-centric approach.</p>
        </div>
        <div class="about-image">
          <div class="image-placeholder"></div>
        </div>
      </div>
    </div>
  </section>

  <section class="mission-vision animate-on-scroll">
    <div class="container">
      <div class="purpose-content">
        <h2>Our Purpose</h2>
        <p><strong>Mission:</strong> To accelerate our clients' success through transformative technology solutions, delivered with uncompromising quality, speed, and integrity.</p>
        <p><strong>Vision:</strong> To be the most trusted and impactful technology partner for businesses navigating critical change and pursuing ambitious goals.</p>
      </div>
    </div>
  </section>

  <section class="values animate-on-scroll">
    <div class="container">
      <h2 class="section-title">Our Core Values</h2>
      <div class="values-grid">
        <div class="value-card">
          <h3>Integrity</h3>
          <p>Upholding the highest ethical standards. Radical honesty and transparency in every interaction with clients and partners.</p>
        </div>
        
        <div class="value-card">
          <h3>Excellence</h3>
          <p>Commitment to best-in-class engineering, strategic thinking, and delivering exceptional, measurable results.</p>
        </div>
        
        <div class="value-card">
          <h3>Partnership</h3>
          <p>Operating as true partners, fostering collaboration, shared ownership, and mutual success with clients and our internal team.</p>
        </div>
        
        <div class="value-card">
          <h3>Agility</h3>
          <p>Embracing a nimble, adaptive approach. Responding quickly to client needs and evolving market dynamics.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="team animate-on-scroll">
    <div class="container">
      <h2 class="section-title">Our Leadership Team</h2>
      <div class="team-grid">
        <div class="team-member">
          <div class="member-image">
            <div class="image-placeholder"></div>
          </div>
          <h3>John Doe</h3>
          <p class="position">Founder & CEO</p>
          <p class="bio">With over 15 years of experience in business consulting, John leads our team with vision and expertise.</p>
        </div>
        
        <div class="team-member">
          <div class="member-image">
            <div class="image-placeholder"></div>
          </div>
          <h3>Jane Smith</h3>
          <p class="position">Chief Strategy Officer</p>
          <p class="bio">Jane brings extensive knowledge in strategic planning and business development to our clients.</p>
        </div>
        
        <div class="team-member">
          <div class="member-image">
            <div class="image-placeholder"></div>
          </div>
          <h3>Michael Johnson</h3>
          <p class="position">Technology Director</p>
          <p class="bio">Michael's expertise in technology solutions helps businesses implement innovative digital strategies.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="cta-section animate-on-scroll">
    <div class="container">
      <h2>Let's Build the Future Together.</h2>
      <p>Discover how a partnership with Anjaneya Innovations can transform your business.</p>
      <a href="/contact" class="btn btn-primary">Get in Touch</a>
    </div>
  </section>
</MainLayout>

<style>
  /* Restyle page header for Dark Mode */
  .page-header {
    /* Use card background, adjust padding */
    background: var(--card-bg-color);
    color: var(--primary-color);
    padding: 5rem 2rem 4rem; 
    text-align: center;
    position: relative;
    overflow: hidden;
    border-bottom: 1px solid var(--border-color);
  }
  
  /* Remove old overlays */
  .page-header::before, 
  .page-header::after {
     display: none; 
  }

  .page-header .container {
    position: relative;
    z-index: 2;
  }

  .page-header h1 {
    font-size: clamp(2.8rem, 5.5vw, 4rem); /* Match global H1 */
    margin-bottom: 0.5rem;
    font-weight: 800;
    color: var(--primary-color);
    text-shadow: none; /* Remove shadow */
  }

  .page-header p {
    font-size: 1.2rem;
    max-width: 700px;
    margin: 0 auto;
    font-weight: 400; /* Regular weight */
    color: var(--secondary-color);
  }

  /* Container - use global */
  .container {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 4rem 2rem; /* Match index.astro */
  }

  /* Apply alternating backgrounds */
  section:nth-of-type(even) {
     background-color: var(--background-color); /* Darkest */
  }
  section:nth-of-type(odd) {
     background-color: var(--card-bg-color); /* Lighter dark */
  }
  section.page-header, section.cta-section {
      background-color: var(--card-bg-color); /* Override header/cta */
  }


  /* About Intro - Dark Mode */
  .about-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); /* Responsive */
    gap: 3rem;
    align-items: center;
  }

  .about-content h2 {
    font-size: clamp(1.8rem, 4vw, 2.2rem); /* Smaller H2 */
    margin-bottom: 1rem;
    color: var(--primary-color); /* Light text */
  }

  .about-content p {
    margin-bottom: 1rem;
    line-height: 1.7;
    color: var(--secondary-color); /* Medium gray text */
  }

  /* Restyle image placeholder */
  .about-image {
      background-color: var(--background-color); /* Darkest BG */
      border-radius: var(--radius-default);
      min-height: 350px; /* Slightly taller */
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      overflow: hidden;
      border: 1px solid var(--border-color);
  }
  
  .image-placeholder {
    width: 100%; /* Apply to both placeholders initially */
    height: 100%;
    /* Remove dashed border etc */
    /* border: 2px dashed var(--border-color); */
    /* display: flex; */
    /* align-items: center; */
    /* justify-content: center; */
    /* color: var(--border-color); */
    /* font-size: 2rem; */
    /* font-weight: bold; */
    opacity: 1; /* Reset opacity */
    padding: 0; 
    background: none; 
    /* Apply Gradient */
    background-color: var(--background-color); /* Fallback */
    background-image: linear-gradient(160deg, rgba(31, 41, 55, 0.4) 0%, rgba(17, 24, 39, 0.6) 100%);
    border-radius: inherit; /* Inherit from parent (.about-image or .member-image) */
  }
  /* Remove placeholder icon */
  /* 
  .image-placeholder::after {
     content: '🖼️';
     opacity: 0.5;
  }
  */

  /* Ensure the main about image placeholder fills height */
  .about-image .image-placeholder {
     padding-bottom: 0; /* Reset padding if needed */
     /* Height is controlled by .about-image min-height */
  }


  /* Mission Vision - Dark Mode */
  .mission-vision {
    /* background handled by nth-of-type */
  }

  .mission-vision-grid {
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
  }

  .mission h2, .vision h2, 
  .purpose-content h2 {
    font-size: clamp(1.6rem, 3.5vw, 2rem); 
    margin-bottom: 1rem;
    color: var(--primary-color);
    border-bottom: 2px solid var(--accent-color); 
    display: inline-block; 
    padding-bottom: 0.3em;
  }

  .mission p, .vision p,
  .purpose-content p {
    font-size: 1rem;
    line-height: 1.7;
    color: var(--secondary-color);
    margin-bottom: 1rem;
  }
  .purpose-content p strong {
      color: var(--primary-color);
      font-weight: 700;
  }

  /* Values - Dark Mode */
  /* Use global section title */
  .section-title {
    font-size: clamp(2.2rem, 5vw, 3rem); 
    margin-bottom: 3rem; 
    text-align: center;
    color: var(--primary-color); /* Light Text */
    font-weight: 800;
    /* Add underline */
    border-bottom: 2px solid var(--accent-color);
    display: inline-block; /* Fit underline */
    padding-bottom: 0.3em;
    margin-bottom: calc(3rem - 0.3em - 2px); /* Adjust margin */
  }

  .values-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem; /* Slightly smaller gap */
  }

  .value-card {
    background-color: var(--card-bg-color); /* Card BG */
    padding: 1.8rem; /* Adjust padding */
    border-radius: var(--radius-default);
    border: 1px solid var(--border-color);
    transition: transform 0.3s ease, border-color 0.3s ease;
    box-shadow: none;
  }

  .value-card:hover {
    transform: translateY(-5px);
    border-color: var(--accent-color);
  }

  .value-card h3 {
    font-size: 1.3rem; /* Adjust size */
    margin-bottom: 0.8rem;
    color: var(--primary-color);
    font-weight: 700; /* Bold */
  }
  
  .value-card p {
      font-size: 0.95rem;
      color: var(--secondary-color);
      line-height: 1.6;
  }

  /* Team - Dark Mode */
  .team {
    /* background handled by nth-of-type */
  }

  .team-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); /* Adjust size */
    gap: 2rem;
  }

  .team-member {
    text-align: center;
    background-color: var(--card-bg-color);
    padding: 2rem;
    border-radius: var(--radius-default);
    border: 1px solid var(--border-color);
    transition: transform 0.3s ease, border-color 0.3s ease;
  }
  
  .team-member:hover {
      transform: translateY(-5px);
      border-color: var(--accent-color);
  }

  .member-image {
    margin-bottom: 1.5rem;
    width: 120px; 
    height: 120px;
    margin-left: auto;
    margin-right: auto;
    border-radius: 50%; /* Make container round */
    overflow: hidden; /* Clip gradient to circle */
    border: 1px solid var(--border-color);
  }

  .member-image .image-placeholder {
    padding-bottom: 0; 
    border-radius: 50%; /* Ensure placeholder itself is round */
    width: 100%;
    height: 100%;
    /* Use a slightly different gradient maybe? */
    background-image: linear-gradient(45deg, rgba(31, 41, 55, 0.2) 0%, rgba(17, 24, 39, 0.4) 100%);
  }

  .team-member h3 {
    font-size: 1.4rem;
    margin-bottom: 0.3rem;
    color: var(--primary-color);
    font-weight: 700;
  }

  .team-member .position {
    color: var(--accent-color); /* Use accent for position */
    font-weight: 600;
    margin-bottom: 0.8rem;
    font-size: 0.9rem;
    display: block;
  }

  .team-member .bio {
    line-height: 1.6;
    font-size: 0.9rem;
    color: var(--secondary-color);
  }

  /* CTA Section - Dark Mode */
  .cta-section {
    background: var(--card-bg-color); 
    color: var(--primary-color); 
    text-align: center;
    position: relative;
    overflow: hidden;
    border-top: 1px solid var(--border-color);
  }
  
  .cta-section::before {
     display: none;
  }
  
  .cta-section h2 {
    font-size: 2.2rem; 
    margin-bottom: 1rem; 
    color: var(--primary-color); 
  }

  .cta-section p {
    font-size: 1.1rem; 
    max-width: 600px; 
    margin: 0 auto 2rem; 
    color: var(--secondary-color); 
  }

  /* Responsive Styles */
  @media (max-width: 992px) {
    .mission-vision-grid {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .mission, .vision {
      text-align: center;
      max-width: 600px;
      margin: 0 auto;
    }
    .mission h2, .vision h2 {
        display: inline-block;
    }
  }

  @media (max-width: 768px) {
    /* page header handled globally */
    
    .about-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    /* container handled globally */

    .about-image {
      order: -1;
      min-height: 250px;
    }
    
    .values-grid {
        grid-template-columns: 1fr; /* Stack values */
    }
    
    .team-grid {
        grid-template-columns: 1fr; /* Stack team */
    }
  }

  /* Define button styles locally */
  .btn {
    display: inline-block;
    padding: 0.9rem 2.2rem; 
    border-radius: var(--radius-default); 
    text-decoration: none;
    font-family: var(--font-sans);
    font-weight: 800; 
    font-size: 0.9rem;
    transition: all 0.2s ease-in-out;
    border: 1px solid transparent;
    cursor: pointer;
    text-align: center;
    letter-spacing: 0.5px;
    text-transform: uppercase; 
  }

  .btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 3px 6px rgba(0,0,0,0.1); 
  }

  .btn-primary {
    /* Default: Dark bg, light text, accent border */
    background-color: var(--card-bg-color);
    color: var(--primary-color);
    border-color: var(--accent-color);
  }

  .btn-primary:hover {
    /* Hover: Cyan bg, dark text */
    background-color: var(--accent-color); 
    border-color: var(--accent-color);
    color: var(--text-on-light);
  }
</style> 
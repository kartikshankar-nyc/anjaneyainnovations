---
import MainLayout from '../layouts/MainLayout.astro';
---

<MainLayout title="About Us - Anjaneya Innovations">
  <section class="page-header">
    <div class="container">
      <h1>About Us</h1>
      <p>Learn more about Anjaneya Innovations and our mission</p>
    </div>
  </section>

  <section class="about-intro animate-on-scroll">
    <div class="container">
      <div class="about-grid">
        <div class="about-content">
          <h2>Our Story</h2>
          <p>Anjaneya Innovations was founded with a vision to provide exceptional consulting services that help businesses navigate the complexities of today's market landscape.</p>
          <p>Named after the Hindu deity Anjaneya (also known as Hanuman), who symbolizes strength, wisdom, and unwavering dedication, our company embodies these values in every service we provide.</p>
          <p>With a team of experienced professionals and a client-centered approach, we have established ourselves as a trusted partner for businesses seeking innovative solutions to drive growth and achieve their objectives.</p>
        </div>
        <div class="about-image">
          <div class="image-placeholder"></div>
        </div>
      </div>
    </div>
  </section>

  <section class="mission-vision animate-on-scroll">
    <div class="container">
      <div class="mission-vision-grid">
        <div class="mission">
          <h2>Our Mission</h2>
          <p>To empower businesses with innovative strategies and solutions that drive sustainable growth and exceptional performance.</p>
        </div>
        <div class="vision">
          <h2>Our Vision</h2>
          <p>To be recognized as the leading consulting firm that transforms businesses through innovation, expertise, and unwavering commitment to client success.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="values animate-on-scroll">
    <div class="container">
      <h2 class="section-title">Our Core Values</h2>
      <div class="values-grid">
        <div class="value-card">
          <h3>Innovation</h3>
          <p>We constantly seek creative solutions and stay ahead of industry trends to provide cutting-edge strategies for our clients.</p>
        </div>
        
        <div class="value-card">
          <h3>Integrity</h3>
          <p>We uphold the highest ethical standards in all our business interactions, building trust and credibility with our clients.</p>
        </div>
        
        <div class="value-card">
          <h3>Excellence</h3>
          <p>We are committed to delivering exceptional quality in every aspect of our services, exceeding client expectations.</p>
        </div>
        
        <div class="value-card">
          <h3>Collaboration</h3>
          <p>We work closely with our clients, fostering partnerships that lead to shared success and sustainable results.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="team animate-on-scroll">
    <div class="container">
      <h2 class="section-title">Our Leadership Team</h2>
      <div class="team-grid">
        <div class="team-member">
          <div class="member-image">
            <div class="image-placeholder"></div>
          </div>
          <h3>John Doe</h3>
          <p class="position">Founder & CEO</p>
          <p class="bio">With over 15 years of experience in business consulting, John leads our team with vision and expertise.</p>
        </div>
        
        <div class="team-member">
          <div class="member-image">
            <div class="image-placeholder"></div>
          </div>
          <h3>Jane Smith</h3>
          <p class="position">Chief Strategy Officer</p>
          <p class="bio">Jane brings extensive knowledge in strategic planning and business development to our clients.</p>
        </div>
        
        <div class="team-member">
          <div class="member-image">
            <div class="image-placeholder"></div>
          </div>
          <h3>Michael Johnson</h3>
          <p class="position">Technology Director</p>
          <p class="bio">Michael's expertise in technology solutions helps businesses implement innovative digital strategies.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="cta-section animate-on-scroll">
    <div class="container">
      <h2>Ready to Work With Us?</h2>
      <p>Contact our team today to discuss how we can help your business succeed.</p>
      <a href="/contact" class="btn btn-primary">Get in Touch</a>
    </div>
  </section>
</MainLayout>

<style>
  /* Restyle page header for Dark Mode */
  .page-header {
    /* Use card background, adjust padding */
    background: var(--card-bg-color);
    color: var(--primary-color);
    padding: 5rem 2rem 4rem; 
    text-align: center;
    position: relative;
    overflow: hidden;
    border-bottom: 1px solid var(--border-color);
  }
  
  /* Remove old overlays */
  .page-header::before, 
  .page-header::after {
     display: none; 
  }

  .page-header .container {
    position: relative;
    z-index: 2;
  }

  .page-header h1 {
    font-size: clamp(2.8rem, 5.5vw, 4rem); /* Match global H1 */
    margin-bottom: 0.5rem;
    font-weight: 800;
    color: var(--primary-color);
    text-shadow: none; /* Remove shadow */
  }

  .page-header p {
    font-size: 1.2rem;
    max-width: 700px;
    margin: 0 auto;
    font-weight: 400; /* Regular weight */
    color: var(--secondary-color);
  }

  /* Container - use global */
  .container {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 4rem 2rem; /* Match index.astro */
  }

  /* Apply alternating backgrounds */
  section:nth-of-type(even) {
     background-color: var(--background-color); /* Darkest */
  }
  section:nth-of-type(odd) {
     background-color: var(--card-bg-color); /* Lighter dark */
  }
  section.page-header, section.cta-section {
      background-color: var(--card-bg-color); /* Override header/cta */
  }


  /* About Intro - Dark Mode */
  .about-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); /* Responsive */
    gap: 3rem;
    align-items: center;
  }

  .about-content h2 {
    font-size: clamp(1.8rem, 4vw, 2.2rem); /* Smaller H2 */
    margin-bottom: 1rem;
    color: var(--primary-color); /* Light text */
  }

  .about-content p {
    margin-bottom: 1rem;
    line-height: 1.7;
    color: var(--secondary-color); /* Medium gray text */
  }

  /* Restyle image placeholder */
  .about-image {
      background-color: var(--background-color); /* Darkest BG */
      border-radius: var(--radius-default);
      min-height: 350px; /* Slightly taller */
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      overflow: hidden;
      border: 1px solid var(--border-color);
  }
  
  .image-placeholder {
    width: 100px;
    height: 100px;
    border: 2px dashed var(--border-color);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--border-color);
    font-size: 2rem;
    font-weight: bold;
    opacity: 0.5;
    padding: 0; /* Reset padding */
    background: none; /* Remove light bg */
  }
  .image-placeholder::after {
     content: '🖼️';
     opacity: 0.5;
  }


  /* Mission Vision - Dark Mode */
  .mission-vision {
    /* background handled by nth-of-type */
  }

  .mission-vision-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); /* Responsive */
    gap: 3rem;
  }

  .mission h2, .vision h2 {
    font-size: clamp(1.6rem, 3.5vw, 2rem); /* Smaller H2 */
    margin-bottom: 1rem;
    color: var(--primary-color);
    border-bottom: 2px solid var(--accent-color); /* Accent underline */
    display: inline-block; /* Fit underline to text */
    padding-bottom: 0.3em;
  }

  .mission p, .vision p {
    font-size: 1rem;
    line-height: 1.7;
    color: var(--secondary-color);
  }

  /* Values - Dark Mode */
  /* Use global section title */
  .section-title {
    font-size: clamp(2.2rem, 5vw, 3rem); 
    margin-bottom: 3rem; 
    text-align: center;
    color: var(--primary-color); /* Light Text */
    font-weight: 800;
  }

  .values-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem; /* Slightly smaller gap */
  }

  .value-card {
    background-color: var(--card-bg-color); /* Card BG */
    padding: 1.8rem; /* Adjust padding */
    border-radius: var(--radius-default);
    border: 1px solid var(--border-color);
    transition: transform 0.3s ease, border-color 0.3s ease;
    box-shadow: none;
  }

  .value-card:hover {
    transform: translateY(-5px);
    border-color: var(--accent-color);
  }

  .value-card h3 {
    font-size: 1.3rem; /* Adjust size */
    margin-bottom: 0.8rem;
    color: var(--primary-color);
    font-weight: 700; /* Bold */
  }
  
  .value-card p {
      font-size: 0.95rem;
      color: var(--secondary-color);
      line-height: 1.6;
  }

  /* Team - Dark Mode */
  .team {
    /* background handled by nth-of-type */
  }

  .team-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); /* Adjust size */
    gap: 2rem;
  }

  .team-member {
    text-align: center;
    background-color: var(--card-bg-color);
    padding: 2rem;
    border-radius: var(--radius-default);
    border: 1px solid var(--border-color);
  }

  .member-image {
    margin-bottom: 1.5rem;
    width: 120px; /* Set size */
    height: 120px;
    margin-left: auto;
    margin-right: auto;
  }

  .member-image .image-placeholder {
    padding-bottom: 0; /* Remove padding */
    border-radius: 50%;
    width: 100%;
    height: 100%;
  }

  .team-member h3 {
    font-size: 1.4rem;
    margin-bottom: 0.3rem;
    color: var(--primary-color);
    font-weight: 700;
  }

  .team-member .position {
    color: var(--accent-color); /* Use accent for position */
    font-weight: 600;
    margin-bottom: 0.8rem;
    font-size: 0.9rem;
    display: block;
  }

  .team-member .bio {
    line-height: 1.6;
    font-size: 0.9rem;
    color: var(--secondary-color);
  }

  /* CTA Section - Dark Mode */
  .cta-section {
    background: var(--card-bg-color); 
    color: var(--primary-color); 
    text-align: center;
    position: relative;
    overflow: hidden;
    border-top: 1px solid var(--border-color);
  }
  
  .cta-section::before {
     display: none;
  }
  
  .cta-section h2 {
    font-size: 2.2rem; 
    margin-bottom: 1rem; 
    color: var(--primary-color); 
  }

  .cta-section p {
    font-size: 1.1rem; 
    max-width: 600px; 
    margin: 0 auto 2rem; 
    color: var(--secondary-color); 
  }

  /* Use global/index button styles */
  .btn {
     /* ... */
  }
  .btn-primary {
     /* ... */
  }

  /* Responsive Styles */
  @media (max-width: 992px) {
    .mission-vision-grid {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .mission, .vision {
      text-align: center;
      max-width: 600px;
      margin: 0 auto;
    }
    .mission h2, .vision h2 {
        display: inline-block;
    }
  }

  @media (max-width: 768px) {
    /* page header handled globally */
    
    .about-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    /* container handled globally */

    .about-image {
      order: -1;
      min-height: 250px;
    }
    
    .values-grid {
        grid-template-columns: 1fr; /* Stack values */
    }
    
    .team-grid {
        grid-template-columns: 1fr; /* Stack team */
    }
  }
</style> 
---
import MainLayout from '../layouts/MainLayout.astro';
---

<MainLayout title="About Us - Anjaneya Innovations">
  <section class="page-header">
    <div class="container">
      <h1>About Anjaneya Innovations</h1>
      <p>Defining the next generation of technology partnership.</p>
    </div>
  </section>

  <section class="about-intro animate-on-scroll">
    <div class="container">
      <div class="about-grid">
        <div class="about-content">
          <h2>Our Approach</h2>
          <p>Anjaneya Innovations was conceived to redefine technology consulting. We operate as a deeply integrated partner, embedding strategic expertise, AI-first thinking, and elite engineering talent directly within our clients' organizations.</p>
          <p><strong>While in stealth, we are dedicated to refining our unique methodologies and establishing foundational client partnerships.</strong></p>
          <p>Our remote-first model ensures agility and global expertise, while radical transparency builds trust and alignment. We achieve exceptional, data-driven outcomes through a principled, client-centric approach.</p>
        </div>
        <!-- Remove about-image div -->
        <!-- 
        <div class="about-image">
          <div class="image-placeholder">
          </div>
        </div>
         -->
      </div>
    </div>
  </section>

  <section class="mission-vision animate-on-scroll">
    <div class="container">
      <div class="purpose-content">
        <h2>Our Purpose</h2>
        <p><strong>Mission:</strong> To accelerate client success through transformative, AI-enhanced technology solutions—delivered with uncompromising quality, speed, and integrity.</p>
        <p><strong>Vision:</strong> To be the most trusted and impactful technology partner for businesses navigating critical change and pursuing ambitious goals in the AI era.</p>
      </div>
    </div>
  </section>

  <section class="values animate-on-scroll">
    <div class="container">
      <h2 class="section-title">Our Core Values</h2>
      <div class="values-grid">
        <div class="value-card">
          <h3>Integrity</h3>
          <p>Upholding the highest ethical standards. Radical honesty and transparency in every interaction with clients and partners.</p>
        </div>
        
        <div class="value-card">
          <h3>Excellence</h3>
          <p>A commitment to best-in-class engineering, strategic thinking, and delivering exceptional, measurable results.</p>
        </div>
        
        <div class="value-card">
          <h3>Partnership</h3>
          <p>Operating as true partners, fostering collaboration, shared ownership, and mutual success with clients and our internal team.</p>
        </div>
        
        <div class="value-card">
          <h3>Agility</h3>
          <p>Embracing a nimble, adaptive approach to quickly meet client needs and respond to evolving market dynamics.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="team animate-on-scroll">
    <div class="container">
      <h2 class="section-title">Our Leadership Team</h2>
      <div class="team-grid">
        <div class="team-member">
          <div class="member-image">
            <div class="image-placeholder">
              <!-- Remove leftover SVG icon -->
            </div>
          </div>
          <h3>John Doe</h3>
          <p class="position">Founder & CEO</p>
          <p class="bio">With over 15 years of experience in business consulting, John leads our team with vision and expertise.</p>
        </div>
        
        <div class="team-member">
          <div class="member-image">
            <div class="image-placeholder">
              <!-- Remove leftover SVG icon -->
            </div>
          </div>
          <h3>Jane Smith</h3>
          <p class="position">Chief Strategy Officer</p>
          <p class="bio">Jane brings extensive knowledge in strategic planning and business development to our clients.</p>
        </div>
        
        <div class="team-member">
          <div class="member-image">
            <div class="image-placeholder">
              <!-- Remove leftover SVG icon -->
            </div>
          </div>
          <h3>Michael Johnson</h3>
          <p class="position">Technology Director</p>
          <p class="bio">Michael's expertise in technology solutions helps businesses implement innovative digital strategies.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="cta-section animate-on-scroll">
    <div class="container">
      <h2>Let's Build the Future Together.</h2>
      <p>Discover how a partnership with Anjaneya Innovations can accelerate your transformation.</p>
      <a href="/contact" class="btn btn-primary">Get in Touch</a>
    </div>
  </section>
</MainLayout>

<style>
  /* Restyle page header for Dark Mode */
  .page-header {
    /* Use card background, adjust padding */
    background: var(--card-bg-color);
    color: var(--primary-color);
    padding: var(--space-3xl) var(--space-xl) var(--space-2xl); 
    text-align: center;
    position: relative;
    overflow: hidden;
    border-bottom: 1px solid var(--border-color);
  }
  
  /* Remove old overlays */
  .page-header::before, 
  .page-header::after {
     display: none; 
  }

  .page-header .container {
    position: relative;
    z-index: 2;
  }

  .page-header h1 {
    font-size: clamp(2.8rem, 5.5vw, 4rem); /* Match global H1 */
    margin-bottom: 0.5rem;
    font-weight: 800;
    color: var(--primary-color);
    text-shadow: none; /* Remove shadow */
  }

  .page-header p {
    font-size: 1.2rem;
    max-width: 700px;
    margin: 0 auto;
    font-weight: 400; /* Regular weight */
    color: var(--secondary-color);
    margin-bottom: var(--space-md);
  }

  /* Container - use global */
  .container {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: var(--space-3xl) var(--space-xl); /* Match index.astro */
  }

  /* Apply alternating backgrounds */
  section:nth-of-type(even) {
     background-color: var(--background-color); /* Darkest */
  }
  section:nth-of-type(odd) {
     background-color: var(--card-bg-color); /* Lighter dark */
  }
  section.page-header, section.cta-section {
      background-color: var(--card-bg-color); /* Override header/cta */
  }


  /* About Intro - Dark Mode */
  .about-grid {
    display: grid;
    /* Change to single column */
    grid-template-columns: 1fr;
    gap: 0; /* Remove gap */
    align-items: center;
  }

  .about-content h2 {
    font-size: clamp(1.8rem, 4vw, 2.2rem); /* Smaller H2 */
    margin-bottom: var(--space-lg);
    color: var(--primary-color); /* Light text */
  }

  .about-content p {
    margin-bottom: var(--space-lg);
    line-height: 1.7;
    color: var(--secondary-color); /* Medium gray text */
  }

  /* Restyle image placeholder */
  .about-image {
      background: none; 
      border-radius: var(--radius-default);
      min-height: 350px; 
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      overflow: hidden;
      border: 1px solid var(--border-color);
      background-color: var(--background-color); /* Apply bg here */
  }
  
  .image-placeholder {
    width: 100%; 
    height: 100%;
    padding: 0; 
    background: none; 
    opacity: 1; 
    /* Remove pattern */
    background-image: none; 
    border-radius: inherit; 
    /* Apply subtle gradient + make relative */
    background: linear-gradient(160deg, var(--background-color) 0%, var(--card-bg-color) 100%);
    position: relative;
    overflow: hidden;
  }

  /* Add inset frame effect */
  .image-placeholder::before {
      content: '';
      position: absolute;
      top: var(--space-md);
      left: var(--space-md);
      right: var(--space-md);
      bottom: var(--space-md);
      border: 2px solid var(--accent-color);
      border-radius: var(--radius-default);
      opacity: 0.3;
  }

  /* Style the SVG icon */
  /* 
  .image-placeholder svg {
      width: 48px; 
      height: 48px;
      stroke: var(--accent-color); 
      opacity: 0.7;
  }
  */

  /* Ensure the main about image placeholder fills height */
  .about-image .image-placeholder {
     padding-bottom: 0; /* Reset padding if needed */
     /* Height is controlled by .about-image min-height */
  }


  /* Mission Vision - Dark Mode */
  .mission-vision {
    /* background handled by nth-of-type */
  }

  .mission-vision-grid {
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
  }

  .mission h2, .vision h2, 
  .purpose-content h2 {
    font-size: clamp(1.6rem, 3.5vw, 2rem); 
    margin-bottom: var(--space-md);
    color: var(--primary-color);
    border-bottom: 2px solid var(--accent-color); 
    display: inline-block; 
    padding-bottom: var(--space-sm);
  }

  .mission p, .vision p,
  .purpose-content p {
    font-size: 1rem;
    line-height: 1.7;
    color: var(--secondary-color);
    margin-bottom: var(--space-md);
  }
  .purpose-content p strong {
      color: var(--primary-color);
      font-weight: 700;
  }

  /* Values - Dark Mode */
  /* Use global section title */
  .section-title {
    font-size: clamp(2.2rem, 5vw, 3rem); 
    padding-bottom: var(--space-sm); 
    margin-bottom: calc(var(--space-2xl) - var(--space-sm) - 2px);
    text-align: center;
    color: var(--primary-color); /* Light Text */
    font-weight: 800;
    /* Add underline */
    border-bottom: 2px solid var(--accent-color);
    display: inline-block; /* Fit underline */
  }

  .values-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--space-lg);
  }

  .value-card {
    background-color: var(--card-bg-color); /* Card BG */
    padding: var(--space-xl); /* Adjust padding */
    border-radius: var(--radius-default);
    border: 1px solid var(--border-color);
    transition: transform 0.3s ease, border-color 0.3s ease;
    box-shadow: none;
  }

  .value-card:hover {
    transform: translateY(-5px);
    border-color: var(--accent-color);
  }

  .value-card h3 {
    font-size: 1.3rem; /* Adjust size */
    margin-bottom: var(--space-sm);
    color: var(--primary-color);
    font-weight: 700; /* Bold */
  }
  
  .value-card p {
      font-size: 0.95rem;
      color: var(--secondary-color);
      line-height: 1.6;
  }

  /* Team - Dark Mode */
  .team {
    /* background handled by nth-of-type */
  }

  .team-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); /* Adjust size */
    gap: var(--space-xl);
  }

  .team-member {
    text-align: center;
    background-color: var(--card-bg-color);
    padding: var(--space-xl);
    border-radius: var(--radius-default);
    border: 1px solid var(--border-color);
    transition: transform 0.3s ease, border-color 0.3s ease;
  }
  
  .team-member:hover {
      transform: translateY(-5px);
      border-color: var(--accent-color);
  }

  .member-image {
    margin-bottom: var(--space-lg);
    width: 120px; 
    height: 120px;
    margin-left: auto;
    margin-right: auto;
    border-radius: 50%; 
    overflow: hidden; 
    border: 1px solid var(--border-color);
    /* Add display flex for centering icon */
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: var(--background-color); /* Darkest background */
  }

  .member-image .image-placeholder {
    padding: 0; 
    border-radius: 50%; 
    width: 100%;
    height: 100%;
    background: none; 
    /* Remove pattern */
    background-image: none;
    /* Apply subtle gradient + make relative */
    background: radial-gradient(circle, var(--card-bg-color) 0%, var(--background-color) 100%);
    position: relative;
    overflow: hidden;
  }
  
  /* Add inset frame effect (smaller for team) */
  .member-image .image-placeholder::before {
      content: '';
      position: absolute;
      top: var(--space-sm); /* Smaller inset */
      left: var(--space-sm);
      right: var(--space-sm);
      bottom: var(--space-sm);
      border: 2px solid var(--accent-color);
      border-radius: 50%; /* Match parent */
      opacity: 0.4;
  }

  .team-member h3 {
    font-size: 1.4rem;
    margin-bottom: var(--space-xs);
    color: var(--primary-color);
    font-weight: 700;
  }

  .team-member .position {
    color: var(--accent-color); /* Use accent for position */
    font-weight: 600;
    margin-bottom: var(--space-sm);
    font-size: 0.9rem;
    display: block;
  }

  .team-member .bio {
    line-height: 1.6;
    font-size: 0.9rem;
    color: var(--secondary-color);
  }

  /* CTA Section - Dark Mode */
  .cta-section {
    background: var(--card-bg-color); 
    color: var(--primary-color); 
    text-align: center;
    position: relative;
    overflow: hidden;
    border-top: 1px solid var(--border-color);
    padding: var(--space-3xl) var(--space-xl); 
  }
  
  .cta-section::before {
     display: none;
  }
  
  .cta-section h2 {
    font-size: 2.2rem; 
    margin-bottom: var(--space-md); 
    color: var(--primary-color); 
  }

  .cta-section p {
    font-size: 1.1rem; 
    max-width: 600px; 
    margin: 0 auto var(--space-xl); 
    color: var(--secondary-color); 
  }

  /* Responsive Styles */
  @media (max-width: 992px) {
    .mission-vision-grid {
      grid-template-columns: 1fr;
      gap: var(--space-xl);
    }

    .mission, .vision {
      text-align: center;
      max-width: 600px;
      margin: 0 auto;
    }
    .mission h2, .vision h2 {
        display: inline-block;
    }
  }

  @media (max-width: 768px) {
    /* page header handled globally */
    
    .about-grid {
      grid-template-columns: 1fr;
      gap: var(--space-xl);
    }

    /* container handled globally */
    .container {
      padding: var(--space-2xl) var(--space-md);
    }

    .about-image {
      order: -1;
      min-height: 250px;
    }
    
    .values-grid {
        grid-template-columns: 1fr; /* Stack values */
        gap: var(--space-lg);
    }
    
    .team-grid {
        grid-template-columns: 1fr; /* Stack team */
        gap: var(--space-lg);
    }
  }

  /* Define button styles locally */
  .btn {
    display: inline-block;
    padding: 0.9rem 2.2rem; 
    border-radius: var(--radius-default); 
    text-decoration: none;
    font-family: var(--font-sans);
    font-weight: 800; 
    font-size: 0.9rem;
    transition: all 0.2s ease-in-out;
    border: 1px solid transparent;
    cursor: pointer;
    text-align: center;
    letter-spacing: 0.5px;
    text-transform: uppercase; 
  }

  .btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 3px 6px rgba(0,0,0,0.1); 
  }

  .btn-primary {
    /* Default: Dark bg, light text, accent border */
    background-color: var(--card-bg-color);
    color: var(--primary-color);
    border-color: var(--accent-color);
  }

  .btn-primary:hover {
    /* Hover: Cyan bg, dark text */
    background-color: var(--accent-color); 
    border-color: var(--accent-color);
    color: var(--text-on-light);
  }
</style> 
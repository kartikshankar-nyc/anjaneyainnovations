---
import MainLayout from '../layouts/MainLayout.astro';
---

<MainLayout title="Anjaneya Innovations - Professional Consulting Services">
  <section class="hero">
    <div class="hero-content">
      <div class="stealth-subtitle">We're building something special</div>
      <h1>Engineering the Future of Business.</h1>
      <p>Anjaneya Innovations partners with ambitious organizations to navigate complex challenges and achieve breakthrough results. Operating currently in stealth mode, we leverage a unique blend of deep technical expertise and strategic business acumen to deliver bespoke, high-impact solutions with agility and integrity.</p>
      <div class="cta-buttons">
        <a href="/services" class="btn btn-primary">Our Services</a>
        <a href="/contact" class="btn btn-secondary">Contact Us</a>
      </div>
    </div>
  </section>

  <section class="services animate-on-scroll">
    <div class="container">
      <h2 class="section-title">Our Services</h2>
      <div class="services-grid">
        <div class="service-card">
          <h3>Business Consulting</h3>
          <p>Strategic guidance to optimize your business operations and increase profitability.</p>
        </div>
        
        <div class="service-card">
          <h3>Technology Solutions</h3>
          <p>Cutting-edge technology implementation to streamline your business processes.</p>
        </div>
        
        <div class="service-card">
          <h3>Data Analytics</h3>
          <p>Comprehensive data analysis to drive informed business decisions.</p>
        </div>
      </div>
      <div class="services-cta">
        <a href="/services" class="btn btn-outline">View All Services</a>
      </div>
    </div>
  </section>

  <section class="about-preview animate-on-scroll">
    <div class="container">
      <div class="about-grid">
        <div class="about-content">
          <h2 class="section-title">Our Story</h2>
          <p>Anjaneya Innovations is a forward-thinking consulting firm dedicated to helping businesses achieve their full potential through innovative solutions and strategic guidance.</p>
          <p>Our team of experts brings years of industry experience to deliver tailored services that address your unique business needs.</p>
          <a href="/about" class="btn btn-text">Learn More About Us <span class="arrow">→</span></a>
        </div>
        <div class="about-image">
          <div class="image-placeholder-graphic"></div>
        </div>
      </div>
    </div>
  </section>

  <section class="testimonials animate-on-scroll">
    <div class="container">
      <h2 class="section-title">What Our Clients Say</h2>
      <div class="testimonial-slider">
        <div class="testimonial">
          <div class="testimonial-content">
            <p>"Anjaneya Innovations transformed our business operations, resulting in a 30% increase in efficiency."</p>
          </div>
          <div class="testimonial-author">
            <p class="name">Jane Smith</p>
            <p class="position">CEO, Example Company</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="cta-section animate-on-scroll">
    <div class="container">
      <h2>Ready to Transform Your Business?</h2>
      <p>Contact us today to discuss how Anjaneya Innovations can help you achieve your business goals.</p>
      <a href="/contact" class="btn btn-primary">Get Started</a>
    </div>
  </section>
</MainLayout>

<style>
  /* Stealth Subtitle - Dark Mode Adjust */
  .stealth-subtitle {
    font-size: 1.1rem; 
    font-weight: 600;
    margin-bottom: 1rem;
    /* Use secondary gray text color */
    color: var(--secondary-color); 
    text-transform: uppercase;
    letter-spacing: 1.5px; 
    opacity: 0.9;
    position: relative;
    display: inline-block;
  }

  .stealth-subtitle::before, 
  .stealth-subtitle::after {
    content: '';
    position: absolute;
    height: 1px;
    background-color: var(--accent-color);
    opacity: 0.6;
    top: 50%;
    width: 25px; /* Adjusted width */
  }

  .stealth-subtitle::before {
    right: 100%;
    margin-right: 10px; /* Adjusted margin */
  }

  .stealth-subtitle::after {
    left: 100%;
    margin-left: 10px; /* Adjusted margin */
  }

  /* Hero Section - Dark Mode */
  .hero {
    /* Use primary dark color */
    background: var(--background-color);
    color: var(--primary-color); /* Light text */
    padding: 7rem 2rem 5rem; /* Adjusted padding */
    text-align: center;
    position: relative;
    overflow: hidden;
    /* No border needed on dark */
    /* border-bottom: 1px solid var(--border-color); */
  }

  /* Add subtle gradient/noise if desired */
  /* 
  .hero::before {
     ...
  }
  */

  .hero-content {
    max-width: 750px; 
    margin: 0 auto;
    text-align: center;
    position: relative;
    z-index: 2;
    padding-top: 0; /* Reset padding */
  }

  .hero h1 {
    font-size: clamp(3rem, 6vw, 4.5rem); /* Larger Hero Heading */
    margin-bottom: 1rem;
    font-family: var(--font-sans);
    font-weight: 800; 
    color: var(--primary-color); /* Light Text */
    line-height: 1.1;
  }

  .hero p {
    font-size: 1.2rem; /* Slightly larger */
    max-width: 650px; 
    margin: 0 auto 2.5rem; 
    opacity: 1; 
    color: var(--secondary-color); /* Medium gray text */
  }

  .cta-buttons {
    display: flex;
    gap: 1rem; /* Adjusted gap */
    justify-content: center;
  }

  /* Define button styles locally */ 
  .btn {
    display: inline-block;
    padding: 0.9rem 2.2rem; 
    border-radius: var(--radius-default); 
    text-decoration: none;
    font-family: var(--font-sans);
    font-weight: 700; /* Revert weight if needed */
    font-size: 0.9rem;
    transition: all 0.2s ease-in-out;
    border: 1px solid transparent;
    cursor: pointer;
    text-align: center;
    letter-spacing: 0.5px;
    text-transform: uppercase; 
  }

  .btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 3px 6px rgba(0,0,0,0.1); 
  }

  .btn-primary {
    /* Default: Dark bg, light text, accent border */
    background-color: var(--card-bg-color);
    color: var(--primary-color);
    border-color: var(--accent-color);
  }

  .btn-primary:hover {
    /* Hover: Cyan bg, dark text */
    background-color: var(--accent-color); 
    border-color: var(--accent-color);
    color: var(--text-on-light);
  }

  .btn-secondary,
  .btn-outline {
    background-color: transparent;
    border-color: var(--primary-color); /* Light border */
    color: var(--primary-color); /* Light text */
  }

  .btn-secondary:hover,
  .btn-outline:hover {
    /* Ensure Corrected Hover: Transparent BG, Accent border/text */
    background-color: transparent;
    color: var(--accent-color);
    border-color: var(--accent-color);
  }

  .btn-text {
    color: var(--primary-color);
    font-weight: 600;
    display: inline-flex;
    align-items: center;
    gap: 0.4rem; 
    background: none;
    padding: 0;
    border: none;
    text-transform: none; 
    letter-spacing: normal;
    font-family: var(--font-sans); 
    text-decoration: underline;
    text-decoration-color: transparent; /* Hide underline by default */
    transition: color 0.2s ease, text-decoration-color 0.2s ease;
  }

  .btn-text .arrow {
    transition: transform 0.2s ease;
    font-size: 1.1em; 
  }

  .btn-text:hover {
    color: var(--accent-color);
    text-decoration-color: var(--accent-color); /* Show underline on hover */
    box-shadow: none; 
    transform: none; 
  }
  
  .btn-text:hover .arrow {
    transform: translateX(3px);
  }

  /* Restore hero specific button styles */
  .hero .btn-secondary {
    border-color: var(--primary-color); /* Light border */
    color: var(--primary-color); /* Light text */
  }
  
  .hero .btn-secondary:hover {
      /* Hover: Cyan background, dark text */
      background-color: var(--accent-color); 
      color: var(--text-on-light);
      border-color: var(--accent-color);
  }

  /* Section Styles - Adjust padding and title */
  .section-title {
    font-size: clamp(2.2rem, 5vw, 3rem); 
    margin-bottom: 3rem; 
    text-align: center;
    color: var(--primary-color); /* Light Text */
    font-weight: 800;
    /* Add underline */
    border-bottom: 2px solid var(--accent-color);
    display: inline-block; /* Fit underline */
    padding-bottom: 0.3em;
    margin-bottom: calc(3rem - 0.3em - 2px); /* Adjust margin */
  }

  .container {
    max-width: var(--max-width); /* Use global max-width */
    margin: 0 auto;
    padding: 4rem 2rem; /* Adjusted padding */
  }
  
  /* Alternating Section Backgrounds - Dark Mode */
  section:nth-of-type(even) {
     background-color: var(--background-color); /* Darkest */
  }
  
  section:nth-of-type(odd) {
     background-color: var(--card-bg-color); /* Lighter dark */
  }
  
  section.hero, section.cta-section {
      background-color: var(--background-color); /* Ensure these are darkest */
  }


  /* Services Section - Dark Mode */
  .services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); /* Adjusted minmax */
    gap: 1.5rem; /* Adjusted gap */
    margin-bottom: 2.5rem; /* Adjusted margin */
  }

  .service-card {
    background-color: var(--card-bg-color); /* Use card background */
    padding: 1.8rem; 
    border-radius: var(--radius-default);
    /* Subtle border */
    border: 1px solid var(--border-color); 
    transition: transform 0.3s ease, border-color 0.3s ease;
    display: flex;
    flex-direction: column;
    box-shadow: none; /* Remove light theme shadow */
  }

  .service-card:hover {
    transform: translateY(-5px);
    border-color: var(--accent-color); /* Highlight border on hover */
  }

  .service-card h3 {
    font-size: 1.4rem; 
    margin-bottom: 0.8rem; 
    color: var(--primary-color); /* Light text */
    font-weight: 800; 
    margin-top: 0.5rem;
  }
  
  .service-card p {
      font-size: 0.95rem;
      color: var(--secondary-color); /* Medium gray text */
      flex-grow: 1; 
      margin-bottom: 1.5rem; 
  }

  .services-cta {
    text-align: center;
    margin-top: 1rem; /* Add margin above button */
  }

  /* About Preview Section */
  .about-preview {
    /* background-color: var(--light-bg); /* Use light-bg from layout */ */
    /* Background handled by nth-of-type */
  }
  
  .about-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); /* Make responsive */
    gap: 3rem; /* Adjusted gap */
    align-items: center;
  }

  .about-content p {
    margin-bottom: 1rem; /* Adjusted margin */
    color: var(--text-color);
    font-size: 1rem;
  }
  
  .about-content .section-title {
     text-align: left; /* Align title left */
     margin-bottom: 1.5rem;
     /* Add underline here too */
     border-bottom: 2px solid var(--accent-color);
     display: inline-block; /* Fit underline */
     padding-bottom: 0.3em;
     margin-bottom: calc(1.5rem - 0.3em - 2px); /* Adjust margin */
  }
  
  .about-image {
      /* Use card bg for placeholder area */
      background-color: var(--card-bg-color); 
      border-radius: var(--radius-default);
      min-height: 300px; 
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      overflow: hidden;
      border: 1px solid var(--border-color);
  }
  
  /* Style the placeholder */
  .image-placeholder-graphic {
     width: 100px;
     height: 100px;
     border: 2px dashed var(--border-color);
     border-radius: 50%;
     display: flex;
     align-items: center;
     justify-content: center;
     color: var(--border-color);
     font-size: 2rem;
     font-weight: bold;
     opacity: 0.5; /* Make placeholder more subtle */
  }
  .image-placeholder-graphic::after {
     content: '+'; /* Simple placeholder symbol */
  }

  /* Testimonials Section - Dark Mode */
  .testimonials {
    /* background-color: var(--primary-color); Use primary color bg */ 
    /* color: var(--white); Text should be white */
    /* Background handled by nth-of-type */
  }
  
  .testimonials .container {
      max-width: 850px; /* Limit width for testimonials */
  }

  .testimonial {
    background-color: var(--card-bg-color); /* Card background */
    padding: 2rem 2.5rem; 
    border-radius: var(--radius-default);
    border: 1px solid var(--border-color); 
    margin: 0 auto;
    position: relative; 
    box-shadow: none; /* Remove shadow */
    /* Add transition */
    transition: transform 0.3s ease, border-color 0.3s ease;
  }
  
  .testimonial:hover {
      transform: translateY(-5px);
      border-color: var(--accent-color);
  }

  .testimonial-content {
    font-size: 1.1rem; 
    font-style: italic;
    margin-bottom: 1.5rem;
    color: var(--primary-color); /* Light text */
  }

  .testimonial-author .name {
    font-weight: 600; 
    margin-bottom: 0.2rem; 
    color: var(--primary-color); /* Light text */
  }

  .testimonial-author .position {
    color: var(--secondary-color); /* Medium gray */
    font-size: 0.9rem;
  }
  
  /* Testimonial Slider Styles (If applicable) */
  .testimonial-slider {
     /* Add styles for slider controls if implemented */
  }

  /* CTA Section - Dark Mode */
  .cta-section {
    /* Use card background for contrast? Or keep darkest? */
    background: var(--card-bg-color); 
    color: var(--primary-color); /* Light text */
    text-align: center;
    position: relative;
    overflow: hidden;
    border-top: 1px solid var(--border-color);
  }

  /* Remove pattern */
  .cta-section::before {
     display: none;
     /*
     content: '';
     ...
     */
  }

  .cta-section h2 {
    font-size: 2.2rem; 
    margin-bottom: 1rem; 
    color: var(--primary-color); /* Light heading */
  }

  .cta-section p {
    font-size: 1.1rem; 
    max-width: 600px; 
    margin: 0 auto 2rem; 
    color: var(--secondary-color); /* Medium gray text */
  }
  
  .cta-section .btn-primary {
     /* CTA button might need slightly different styling? */
     /* Example: Use the orange accent */
     /* background-color: var(--accent-orange); */
     /* border-color: var(--accent-orange); */
     /* color: var(--primary-color); */ 
  }
  
  .cta-section .btn-primary:hover {
     /* background-color: #E57A00; */ /* Darker orange */
     /* border-color: #E57A00; */
  }


  /* Responsive Styles */
  @media (max-width: 992px) { /* Add breakpoint for tablets */
      .hero h1 { font-size: 2.5rem; }
      .hero p { font-size: 1.1rem; max-width: 90%; }
      .section-title { font-size: 1.8rem; }
      .container { padding: 3rem 1.5rem; }
  }
  
  
  @media (max-width: 768px) {
    .hero h1 {
      font-size: 2rem; /* Further adjust hero size */
    }
    
    .hero p {
        font-size: 1rem;
    }
    
    .cta-buttons {
       flex-direction: column; /* Stack buttons */
       gap: 0.8rem;
       align-items: center;
    }
    
    .btn {
       padding: 0.8rem 1.5rem;
       width: 80%; /* Make buttons wider */
       max-width: 300px;
    }

    .about-grid {
      grid-template-columns: 1fr; /* Stack about section */
      gap: 2rem;
      text-align: center; /* Center text content */
    }
    
    .about-content .section-title {
        text-align: center; /* Center title */
    }

    .about-image {
      order: -1; /* Image first on mobile */
      min-height: 250px;
    }
    
    .container {
      padding: 3rem 1rem; /* Adjust padding */
    }
    
    .services-grid {
       grid-template-columns: 1fr; /* Single column for services */
    }
    
    .section-title {
        font-size: 1.6rem;
    }
    
    .cta-section h2 {
        font-size: 1.8rem;
    }
    
    .cta-section p {
        font-size: 1rem;
    }
  }
</style>

<script>
  // Add GSAP animation for the logo if GSAP is available
  document.addEventListener('DOMContentLoaded', () => {
    if (typeof gsap !== 'undefined') {
      gsap.from('.hero-logo', {
        duration: 1.5,
        opacity: 0,
        y: -50,
        rotation: -10,
        ease: 'elastic.out(1, 0.5)'
      });
    }
  });
</script> 
---
import MainLayout from '../layouts/MainLayout.astro';
---

<MainLayout title="Our Capabilities - Anjaneya Innovations">
  <section class="page-header">
    <div class="container">
      <h1>Our Capabilities</h1>
      <p>Bespoke solutions designed for transformative results.</p>
      <div class="stealth-banner">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="12" cy="12" r="10"></circle>
          <line x1="12" y1="8" x2="12" y2="12"></line>
          <line x1="12" y1="16" x2="12.01" y2="16"></line>
        </svg>
        <span>Some services are currently in development and will be fully revealed soon</span>
      </div>
    </div>
  </section>

  <section class="services-detail">
    <div class="container">
      <div class="service-item animate-on-scroll">
        <div class="service-content">
          <h2>Strategic Transformation</h2>
          <p>We partner with leadership teams to architect and execute complex organizational turnarounds, drive impactful digital transformation, and foster sustainable growth through optimized structures and processes.</p>
          <ul class="service-features">
            <li>Organizational Turnaround</li>
            <li>Digital Transformation Strategy</li>
            <li>Change Management</li>
            <li>Cost Optimization</li>
            <li>Scaling Results</li>
          </ul>
        </div>
        <div class="service-image">
          <div class="image-placeholder"></div>
        </div>
      </div>

      <div class="service-item animate-on-scroll">
        <div class="service-content">
          <h2>Technology Leadership & Execution</h2>
          <p>Providing expert fractional leadership (CTO, CDO, CISO, Chief Architect) and end-to-end custom software development. We modernize legacy systems, architect robust multi-cloud solutions, and implement best-in-class engineering practices from API strategy to DevSecOps.</p>
          <ul class="service-features">
            <li>Fractional C-Suite Leadership</li>
            <li>Custom Software Development</li>
            <li>Cloud Native Architecture (AWS, Azure, GCP)</li>
            <li>Legacy System Modernization</li>
            <li>API Strategy & Development</li>
            <li>DevSecOps & SRE Practices</li>
            <li>Cybersecurity Strategy & Implementation</li>
            <li>Rapid Prototyping</li>
            <li>UX Consulting & Strategy</li>
            <li>End-to-End UI Design</li>
          </ul>
        </div>
        <div class="service-image">
          <div class="image-placeholder"></div>
        </div>
      </div>

      <div class="service-item animate-on-scroll">
        <div class="service-content">
          <h2>Data & AI Solutions</h2>
          <p>Unlock the power of your data. We design and build modern data platforms, implement advanced analytics and business intelligence, and leverage AI/ML to create predictive insights and competitive advantages.</p>
          <ul class="service-features">
            <li>Data Engineering & Platform Builds</li>
            <li>Business Intelligence & Analytics</li>
            <li>Data Visualization & Reporting</li>
            <li>AI/ML Implementation</li>
            <li>Customer & Market Insights</li>
          </ul>
        </div>
        <div class="service-image">
          <div class="image-placeholder"></div>
        </div>
      </div>

      <div class="service-item animate-on-scroll">
        <div class="service-content">
          <h2>Talent & Team Augmentation</h2>
          <p>Building high-performing teams is critical. We assist with strategic hiring, team building, and providing specialized engineering talent to augment your capabilities and accelerate delivery.</p>
          <ul class="service-features">
            <li>Strategic Hiring Support</li>
            <li>Team Building & Development</li>
            <li>On-Demand Engineering Talent</li>
            <li>ODC/GCC Setup Guidance</li>
          </ul>
        </div>
        <div class="service-image">
          <div class="image-placeholder"></div>
        </div>
      </div>
    </div>
  </section>

  <section class="process animate-on-scroll">
    <div class="container">
      <h2 class="section-title">Our Approach</h2>
      <div class="process-steps">
        <div class="process-step">
          <div class="step-number">1</div>
          <h3>Discovery</h3>
          <p>Understand your business goals, context, challenges, and unique requirements.</p>
        </div>
        
        <div class="process-step">
          <div class="step-number">2</div>
          <h3>Analysis</h3>
          <p>Analyze your current state, market landscape, and identify key opportunities.</p>
        </div>
        
        <div class="process-step">
          <div class="step-number">3</div>
          <h3>Strategy</h3>
          <p>Design bespoke, practical, and high-impact solutions aligned with your objectives.</p>
        </div>
        
        <div class="process-step">
          <div class="step-number">4</div>
          <h3>Implementation</h3>
          <p>Execute with precision, agility, and seamless integration with your teams.</p>
        </div>
        
        <div class="process-step">
          <div class="step-number">5</div>
          <h3>Evaluation</h3>
          <p>Continuously measure results, refine approaches, and ensure tangible value delivery.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="cta-section animate-on-scroll">
    <div class="container">
      <h2>Ready to Transform Your Business?</h2>
      <p>Schedule a consultation to learn how our services can help you achieve your business goals.</p>
      <a href="/contact" class="btn btn-primary">Contact Us Today</a>
    </div>
  </section>
</MainLayout>

<style>
  /* Remove old Stealth Mode Styling */
  /* 
  .stealth-indicator { ... }
  .stealth-badge { ... }
  .stealth-pulse { ... }
  @keyframes pulse { ... }
  */

  /* Restyle page header for Dark Mode */
  .page-header {
    /* Use card background, add padding */
    background: var(--card-bg-color);
    color: var(--primary-color);
    padding: 5rem 2rem 4rem; /* Adjust padding */
    text-align: center;
    position: relative;
    overflow: hidden;
    border-bottom: 1px solid var(--border-color);
  }
  
  /* Remove old overlays */
  .page-header::before, 
  .page-header::after {
     display: none; 
  }

  .page-header .container {
    position: relative;
    z-index: 2;
  }

  .page-header h1 {
    font-size: clamp(2.8rem, 5.5vw, 4rem); /* Match global H1 */
    margin-bottom: 0.5rem;
    font-weight: 800;
    color: var(--primary-color);
    text-shadow: none; /* Remove shadow */
  }

  .page-header p {
    font-size: 1.2rem;
    max-width: 700px;
    margin: 0 auto 1.5rem auto; /* Add bottom margin */
    font-weight: 400; /* Regular weight */
    color: var(--secondary-color);
  }
  
  /* Restyle stealth banner within header */
  .stealth-banner {
    margin-top: 1.5rem;
    display: inline-flex; /* Use inline-flex */
    align-items: center;
    gap: 10px;
    background-color: var(--background-color); /* Darker bg */
    padding: 8px 15px;
    border-radius: var(--radius-default);
    border: 1px solid var(--border-color);
    color: var(--secondary-color);
    font-size: 0.9rem;
  }
  
  .stealth-banner svg {
      stroke: var(--secondary-color);
      width: 16px;
      height: 16px;
  }

  /* Restyle Coming Soon elements */
  /* .coming-soon { ... } */
  /* .coming-soon-badge { ... } */
  /* .exclusive-note { ... } */

  /* Use standard button styles - rely on global */
  .btn-sm {
    padding: 0.6rem 1.2rem;
    font-size: 0.85rem;
  }

  /* Container - use global */
  .container {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 4rem 2rem; /* Match index.astro */
  }

  /* Services Detail - Dark Mode */
  .services-detail {
      background-color: var(--card-bg-color); /* Lighter dark bg */
  }
  
  .service-item {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); /* Responsive grid */
    gap: 3rem;
    margin-bottom: 4rem;
    align-items: start; /* Align items to top */
    direction: ltr !important; /* Override RTL */
  }

  .service-item:last-child {
      margin-bottom: 0;
  }

  /* Remove RTL alternation */
  /* 
  .service-item:nth-child(even) {
    direction: rtl;
  }
  .service-item:nth-child(even) .service-content {
    direction: ltr;
  }
  */

  .service-content h2 {
    font-size: clamp(1.8rem, 4vw, 2.2rem); /* Slightly smaller H2 */
    margin-bottom: 1rem;
    color: var(--primary-color); /* Light text */
  }

  .service-content p {
    margin-bottom: 1.5rem;
    line-height: 1.7;
    color: var(--secondary-color);
  }

  .service-features {
    padding-left: 1.5rem;
    list-style: none; /* Remove default list style */
    margin-top: 1rem;
  }

  .service-features li {
    margin-bottom: 0.7rem;
    position: relative;
    font-size: 0.95rem;
    color: var(--secondary-color);
  }

  .service-features li::before {
    content: "→"; /* Use arrow */
    color: var(--accent-color); /* Cyan arrow */
    font-weight: bold;
    display: inline-block; 
    width: 1em;
    margin-left: -1.1em; /* Adjust spacing */
    font-size: 1.1em;
    line-height: 1;
  }

  .service-image {
      /* Add some styling */
      border-radius: var(--radius-default);
      overflow: hidden;
      border: 1px solid var(--border-color);
  }
  
  .image-placeholder {
    width: 100%;
    padding-bottom: 75%;
    background-color: var(--background-color); /* Darkest bg */
    border-radius: 0; /* Remove radius if inside bordered container */
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--border-color);
    font-size: 2rem;
  }
  /* Add placeholder icon */
  .image-placeholder::after {
      content: '🖼️';
      opacity: 0.5;
  }

  /* Process Section - Dark Mode */
  .process {
    background-color: var(--background-color); /* Darkest bg */
    border-top: 1px solid var(--border-color);
    border-bottom: 1px solid var(--border-color);
  }

  /* Inherit section title from index.astro if possible, or redefine */
  .section-title {
    font-size: clamp(2.2rem, 5vw, 3rem); 
    margin-bottom: 3rem; 
    text-align: center;
    color: var(--primary-color); /* Light Text */
    font-weight: 800;
    /* Add underline */
    border-bottom: 2px solid var(--accent-color);
    display: inline-block; /* Fit underline */
    padding-bottom: 0.3em;
    margin-bottom: calc(3rem - 0.3em - 2px); /* Adjust margin */
  }

  .process-steps {
    display: grid; /* Use grid */
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 1.5rem;
    text-align: center;
  }

  .process-step {
      background-color: var(--card-bg-color); /* Card bg */
      padding: 1.5rem;
      border-radius: var(--radius-default);
      border: 1px solid var(--border-color);
      /* Add transition */
      transition: transform 0.3s ease, border-color 0.3s ease;
  }
  
  .process-step:hover {
      transform: translateY(-5px);
      border-color: var(--accent-color);
  }

  .step-number {
    width: 40px;
    height: 40px;
    line-height: 40px;
    border-radius: 50%;
    background-color: var(--accent-color);
    color: var(--text-on-light); /* Dark text */
    font-weight: 700;
    margin: 0 auto 1rem auto;
    font-size: 1.1rem;
  }

  .process-step h3 {
    font-size: 1.2rem;
    margin-bottom: 0.5rem;
    color: var(--primary-color);
    font-weight: 700;
  }
  
  .process-step p {
      font-size: 0.9rem;
      color: var(--secondary-color);
      max-width: none; /* Allow text to fill */
      margin-bottom: 0;
  }

  /* CTA Section - Use styles from index.astro */
  .cta-section {
    background: var(--card-bg-color); 
    color: var(--primary-color); 
    text-align: center;
    position: relative;
    overflow: hidden;
    border-top: 1px solid var(--border-color);
  }

  .cta-section::before {
     display: none;
  }

  .cta-section h2 {
    font-size: 2.2rem; 
    margin-bottom: 1rem; 
    color: var(--primary-color); 
  }

  .cta-section p {
    font-size: 1.1rem; 
    max-width: 600px; 
    margin: 0 auto 2rem; 
    color: var(--secondary-color); 
  }
  
  /* Responsive Styles */
  @media (max-width: 768px) {
    .service-item {
      grid-template-columns: 1fr; /* Stack on mobile */
      gap: 2rem;
      margin-bottom: 3rem;
    }
    .service-item:nth-child(even) .service-image {
      order: -1; /* Image first */
    }
  }

  /* Define button styles locally */
  .btn {
    display: inline-block;
    padding: 0.9rem 2.2rem; 
    border-radius: var(--radius-default); 
    text-decoration: none;
    font-family: var(--font-sans);
    font-weight: 800; 
    font-size: 0.9rem;
    transition: all 0.2s ease-in-out;
    border: 1px solid transparent;
    cursor: pointer;
    text-align: center;
    letter-spacing: 0.5px;
    text-transform: uppercase; 
  }

  .btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 3px 6px rgba(0,0,0,0.1); 
  }

  .btn-primary {
    /* Default: Dark bg, light text, accent border */
    background-color: var(--card-bg-color);
    color: var(--primary-color);
    border-color: var(--accent-color);
  }

  .btn-primary:hover {
    /* Hover: Cyan bg, dark text */
    background-color: var(--accent-color); 
    border-color: var(--accent-color);
    color: var(--text-on-light);
  }
  
  .btn-secondary,
  .btn-outline {
    background-color: transparent;
    border-color: var(--primary-color); /* Light border */
    color: var(--primary-color); /* Light text */
  }
  
  .btn-secondary:hover,
  .btn-outline:hover {
    /* Corrected Hover: Transparent BG, Accent border/text */
    background-color: transparent;
    color: var(--accent-color);
    border-color: var(--accent-color);
  }

</style> 
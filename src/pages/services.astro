---
import MainLayout from '../layouts/MainLayout.astro';
---

<MainLayout title="Our Capabilities - Anjaneya Innovations">
  <section class="page-header">
    <div class="container">
      <h1>Our Capabilities</h1>
      <p>Bespoke, AI-driven technology solutions designed for transformative results.</p>
      <div class="stealth-banner">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="12" cy="12" r="10"></circle>
          <line x1="12" y1="8" x2="12" y2="12"></line>
          <line x1="12" y1="16" x2="12.01" y2="16"></line>
        </svg>
        <span>Some services are currently in development and will be fully revealed soon</span>
      </div>
    </div>
  </section>

  <section class="services-detail">
    <div class="container">
      <p class="section-intro">We deliver end-to-end technology solutions, integrating AI and modern engineering practices across core business functions and specialized domains.</p>
      
      <!-- Service 1: Strategic Transformation -->
      <div class="service-item animate-on-scroll">
        <div class="service-content">
          <h2>Strategic Transformation</h2>
          <p>We partner with leadership to architect and execute complex organizational turnarounds, drive impactful digital transformation, and foster sustainable growth through optimized structures, processes, and SDLC modernization.</p>
          <ul class="service-features">
            <li>Organizational Turnaround</li>
            <li>Digital Transformation Strategy</li>
            <li>Change Management</li>
            <li>Cost Optimization</li>
            <li>Scaling Results</li>
            <li>SDLC Transformation</li>
          </ul>
        </div>
      </div>

      <!-- Service 2: Technology Leadership & Execution -->
      <div class="service-item animate-on-scroll">
        <div class="service-content">
          <h2>Technology Leadership & Execution</h2>
          <p>Expert fractional leadership (CTO, CDO, CISO, Chief Architect) and end-to-end custom software development. We modernize legacy systems, design compelling user experiences, architect robust multi-cloud solutions (AWS, Azure, GCP), and implement elite engineering practices—from API strategy to DevSecOps, SRE, and test engineering transformation.</p>
          <ul class="service-features">
            <li>Fractional C-Suite Leadership</li>
            <li>Custom Software Development</li>
            <li>Cloud Native Architecture (AWS, Azure, GCP)</li>
            <li>Legacy System Modernization</li>
            <li>Mobile App Development (iOS & Android)</li>
            <li>Rapid Prototyping</li>
            <li>UX Consulting & Strategy</li>
            <li>End-to-End UI Design</li>
            <li>API Strategy & Development</li>
            <li>DevSecOps & SRE Practices</li>
            <li>Test Engineering Transformation</li>
            <li>Cybersecurity Strategy & Implementation</li>
            <li>Rapid Prototyping</li>
            <li>UX Consulting & Strategy</li>
            <li>End-to-End UI Design</li>
            <li>Developer Experience (DevEx / DX) Enhancement</li>
            <li>Engineering Productivity Improvement</li>
            <li>Software Intelligence Platform Implementation</li>
          </ul>
        </div>
      </div>

      <!-- Service 3: Data & AI Solutions -->
      <div class="service-item animate-on-scroll">
        <div class="service-content">
          <h2>Data & AI Solutions</h2>
          <p>Unlock the power of your data ecosystem. We design and build modern data platforms, implement advanced analytics and business intelligence, and integrate AI/ML to create predictive insights and durable competitive advantages.</p>
          <ul class="service-features">
            <li>Data Engineering & Platform Builds</li>
            <li>Business Intelligence & Analytics</li>
            <li>Data Visualization & Reporting</li>
            <li>AI/ML Implementation & Integration</li>
            <li>Customer & Market Insights</li>
          </ul>
        </div>
      </div>

      <!-- Service 4: Financial Services Technology -->
      <div class="service-item animate-on-scroll">
        <div class="service-content">
          <h2>Financial Services Technology</h2>
          <p>Deep domain expertise in architecting and implementing mission-critical technology for the financial sector. We leverage AI for enhanced client intelligence, operational efficiency, and robust solutions across Investment Banking, Wealth Management, and Global Capital Markets.</p>
          <ul class="service-features">
            <li>Investment Banking Platforms</li>
            <li>Wealth Management Technology</li>
            <li>Global Capital Markets Systems</li>
            <li>CRM & Client Intelligence (AI-Enhanced)</li>
            <li>Compliance & Regulatory Tech</li>
          </ul>
        </div>
      </div>

      <!-- Service 5: Salesforce Implementation & Optimization -->
      <div class="service-item animate-on-scroll">
        <div class="service-content">
          <h2>Salesforce Implementation & Optimization</h2>
          <p>Maximize CRM effectiveness with end-to-end custom Salesforce implementations. We streamline workflows, integrate enterprise systems, and leverage AI-powered insights to provide actionable client intelligence tailored to your business.</p>
          <ul class="service-features">
            <li>Custom Salesforce Development (Apex, LWC)</li>
            <li>Sales Cloud, Service Cloud, Experience Cloud</li>
            <li>Integration with Enterprise Systems</li>
            <li>AI-Powered Insights (Einstein Analytics)</li>
            <li>Org Optimization & Health Checks</li>
          </ul>
        </div>
      </div>

      <!-- Service 6: Talent & Team Augmentation -->
      <div class="service-item animate-on-scroll">
        <div class="service-content">
          <h2>Talent & Team Augmentation</h2>
          <p>Building high-performing technology teams is critical. We assist with strategic hiring, effective team building, and providing specialized engineering talent to augment your capabilities and accelerate critical delivery roadmaps.</p>
          <ul class="service-features">
            <li>Strategic Hiring Support</li>
            <li>Team Building & Development</li>
            <li>On-Demand Engineering Talent</li>
            <li>ODC/GCC Setup Guidance</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <section class="process animate-on-scroll">
    <div class="container">
      <h2 class="section-title">Our Approach</h2>
      <div class="process-steps">
        <div class="process-step">
          <div class="step-number">1</div>
          <h3>Discovery</h3>
          <p>Understand your business goals, context, challenges, and unique requirements.</p>
        </div>
        
        <div class="process-step">
          <div class="step-number">2</div>
          <h3>Analysis</h3>
          <p>Analyze your current state, market landscape, and identify key opportunities.</p>
        </div>
        
        <div class="process-step">
          <div class="step-number">3</div>
          <h3>Strategy</h3>
          <p>Design bespoke, practical, and high-impact solutions rigorously aligned with your objectives.</p>
        </div>
        
        <div class="process-step">
          <div class="step-number">4</div>
          <h3>Implementation</h3>
          <p>Execute with precision, agility, and seamless integration with your teams.</p>
        </div>
        
        <div class="process-step">
          <div class="step-number">5</div>
          <h3>Evaluation</h3>
          <p>Continuously measure results, refine approaches, and ensure tangible value delivery.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="cta-section animate-on-scroll">
    <div class="container">
      <h2>Ready to Transform Your Business?</h2>
      <p>Schedule a consultation to learn how our services can help you achieve your business goals.</p>
      <a href="/contact" class="btn btn-primary">Contact Us Today</a>
    </div>
  </section>
</MainLayout>

<style>
  /* Remove old Stealth Mode Styling */
  /* 
  .stealth-indicator { ... }
  .stealth-badge { ... }
  .stealth-pulse { ... }
  @keyframes pulse { ... }
  */

  /* Restyle page header for Dark Mode */
  .page-header {
    /* Remove background */
    /* background: var(--card-bg-color); */
    color: var(--primary-color);
    /* Adjust padding */
    padding: var(--space-3xl) var(--space-xl) var(--space-2xl); 
    text-align: center;
    position: relative;
    overflow: hidden;
    border-bottom: 1px solid var(--border-color);
  }
  
  /* Remove old overlays */
  .page-header::before, 
  .page-header::after {
     display: none; 
  }

  .page-header .container {
    position: relative;
    z-index: 2;
  }

  .page-header h1 {
    font-size: clamp(2.8rem, 5.5vw, 4rem); 
    margin-bottom: 0.5rem;
    font-weight: 800;
    color: var(--primary-color);
    text-shadow: none; 
    /* Add underline */
    border-bottom: 2px solid var(--accent-color);
    display: inline-block; /* Center alignment is default for header */
    padding-bottom: var(--space-xs);
  }

  .page-header p {
    font-size: 1.2rem;
    max-width: 700px;
    /* Adjust margin */
    margin: 0 auto var(--space-lg) auto; 
    font-weight: 400; /* Regular weight */
    color: var(--secondary-color);
  }
  
  /* Restyle stealth banner within header */
  .stealth-banner {
    /* Adjust margin */
    margin-top: var(--space-lg);
    display: inline-flex; /* Use inline-flex */
    align-items: center;
    /* Adjust gap */
    gap: var(--space-sm); 
    background-color: var(--background-color); /* Darker bg */
    /* Adjust padding */
    padding: var(--space-sm) var(--space-md);
    border-radius: var(--radius-default);
    border: 1px solid var(--border-color);
    color: var(--secondary-color);
    font-size: 0.9rem;
  }
  
  .stealth-banner svg {
      stroke: var(--secondary-color);
      width: 16px;
      height: 16px;
  }

  /* Restyle Coming Soon elements */
  .coming-soon {
    position: relative;
    opacity: 0.8; 
    border-left: 3px solid var(--accent-color); 
    /* Adjust padding/margin */
    padding-left: var(--space-xl); 
    margin-left: calc(-1 * var(--space-xl)); 
  }

  .coming-soon-badge {
    position: absolute;
    /* Adjust position */
    top: var(--space-md); 
    left: calc(-1 * var(--space-xl)); 
    background: var(--accent-color);
    color: var(--text-on-light); 
    /* Adjust padding */
    padding: var(--space-xs) var(--space-sm);
    border-radius: var(--radius-default);
  }

  .exclusive-note {
    /* Adjust margin/padding */
    margin-top: var(--space-lg);
    padding: var(--space-lg);
    background-color: var(--background-color); 
    border-left: 3px solid var(--accent-color);
  }

  .exclusive-note p {
    /* Adjust margin */
    margin-bottom: var(--space-md);
    font-style: normal; 
    font-size: 0.95rem;
  }

  /* Use standard button styles - rely on global */
  .btn-sm {
    /* Adjust padding */
    padding: var(--space-sm) var(--space-md);
    font-size: 0.85rem;
  }

  /* Container - use global */
  .container {
    max-width: var(--max-width);
    margin: 0 auto;
    /* Adjust padding */
    padding: var(--space-3xl) var(--space-xl); 
  }

  /* Services Detail - Dark Mode */
  .services-detail {
      /* Remove background */
      /* background-color: var(--card-bg-color); */
  }
  
  /* Remove Alternating Services Layout Styles */
  /*
  .alternating-services-layout { ... }
  .alternating-services-layout::before { ... }
  */

  .service-item {
      /* Revert to simpler layout */
      display: block; /* Standard block flow */
      /* grid-template-columns: 1fr var(--space-2xl) 1fr; */
      /* gap: 0; */ 
      margin-bottom: var(--space-2xl);
      /* align-items: start; */ 
      border: none;
      background: none;
      padding: 0;
      /* Add bottom border for separation */
      border-bottom: 1px solid var(--border-color);
      padding-bottom: var(--space-xl);
  }
  
  .service-item:last-child {
      margin-bottom: 0;
      border-bottom: none; /* No border on last item */
      padding-bottom: 0;
  }
  
  .service-content {
      /* Reset grid positioning */
      grid-column: auto; 
      position: relative; 
      padding: 0; /* Remove padding */
      background: none; /* Remove card background */
      border-radius: 0;
      border: none; /* Remove card border */
      transition: none; /* Remove transitions */
      z-index: auto; 
      /* Reset margins */
      margin-left: 0;
      margin-right: 0;
  }
  
  /* Remove positioning styles */
  /*
  .service-item:nth-child(odd) .service-content { ... }
  .service-item:nth-child(even) .service-content { ... }
  */

  /* Remove hover effects */
  /*
  .service-item:hover .service-content { ... }
  .service-item:hover .service-content h2, ... { ... }
  .service-item:hover .service-content .service-features li::before { ... }
  */

  .service-content h2 {
    font-size: clamp(1.8rem, 4vw, 2.2rem); 
    margin-bottom: var(--space-md);
    color: var(--primary-color); 
    /* Remove underline from here */
    /* border-bottom: 2px solid var(--accent-color); */
    /* display: inline-block; */
    /* padding-bottom: var(--space-sm); */
  }

  .service-content p {
    /* Adjust margin */
    margin-bottom: var(--space-lg);
    line-height: 1.7;
    color: var(--secondary-color);
  }

  .service-features {
    padding-left: 1.5rem;
    list-style: none; /* Remove default list style */
    /* Adjust margin */
    margin-top: var(--space-md);
  }

  .service-features li {
    /* Adjust margin */
    margin-bottom: var(--space-sm);
    position: relative;
    font-size: 0.95rem;
    color: var(--secondary-color);
  }

  .service-features li::before {
    content: "→"; /* Use arrow */
    color: var(--accent-color); /* Cyan arrow */
    font-weight: bold;
    display: inline-block;
    /* Increase width for more space */
    width: 1.5em;
    /* Adjust margin to compensate for width */
    margin-left: -1.6em; 
    /* Increase font size for prominence */
    font-size: 1.3em;
    line-height: 1;
  }

  /* Process Section - Dark Mode */
  .process {
    /* Remove background */
    /* background-color: var(--background-color); */
    border-top: 1px solid var(--border-color);
    border-bottom: 1px solid var(--border-color);
  }

  /* Apply consistent style to section title */
  .section-title {
    font-size: clamp(2.2rem, 5vw, 3rem); 
    padding-bottom: var(--space-xs); 
    margin-bottom: calc(var(--space-2xl) - var(--space-xs) - 2px); 
    text-align: center;
    color: var(--primary-color); 
    font-weight: 800;
    /* Ensure underline */
    border-bottom: 2px solid var(--accent-color);
    display: inline-block; 
  }

  .process-steps {
    display: grid; /* Use grid */
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    /* Adjust gap */
    gap: var(--space-lg); 
    text-align: center;
  }

  .process-step {
      background-color: var(--card-bg-color); /* Card bg */
      padding: var(--space-lg);
      border-radius: var(--radius-default);
      border: 1px solid var(--border-color);
      /* Add transition */
      transition: transform 0.3s ease, border-color 0.3s ease;
  }
  
  .process-step:hover {
      transform: translateY(-5px);
      border-color: var(--accent-color);
  }

  .step-number {
    width: 40px;
    height: 40px;
    line-height: 40px;
    border-radius: 50%;
    background-color: var(--accent-color);
    color: var(--text-on-light); /* Dark text */
    font-weight: 700;
    /* Adjust margin */
    margin: 0 auto var(--space-md) auto;
    font-size: 1.1rem;
  }

  .process-step h3 {
    font-size: 1.2rem;
    margin-bottom: var(--space-sm);
    color: var(--primary-color);
    font-weight: 700;
  }
  
  .process-step p {
      font-size: 0.9rem;
      color: var(--secondary-color);
      max-width: none; /* Allow text to fill */
      margin-bottom: 0;
  }

  /* CTA Section - Use styles from index.astro */
  .cta-section {
    /* Remove background */
    /* background: var(--card-bg-color); */ 
    color: var(--primary-color); 
    text-align: center;
    position: relative;
    overflow: hidden;
    border-top: 1px solid var(--border-color);
    /* Adjust padding */
    padding: var(--space-3xl) var(--space-xl); 
  }
  
  .cta-section::before {
     display: none;
  }

  .cta-section h2 {
    font-size: 2.2rem; 
    /* Adjust margin */
    margin-bottom: var(--space-md); 
    color: var(--primary-color); 
  }

  .cta-section p {
    font-size: 1.1rem; 
    max-width: 600px; 
    /* Adjust margin */
    margin: 0 auto var(--space-xl); 
    color: var(--secondary-color); 
  }

  /* Responsive Styles */
  @media (max-width: 768px) {
    /* Collapse to single column on mobile */
    .alternating-services-layout {
        padding: 0; /* Remove padding */
        &::before {
            display: none; /* Hide central line */
        }
    }

    .service-item {
      grid-template-columns: 1fr; /* Single column */
      margin-bottom: var(--space-xl);
    }
    
    .service-item:nth-child(odd) .service-content,
    .service-item:nth-child(even) .service-content {
        grid-column: 1 / -1; /* Span full width */
        justify-self: auto; /* Reset alignment */
        margin-left: 0;
        margin-right: 0;
    }
    .coming-soon {
        padding-left: 0;
        margin-left: 0;
        border-left: none;
        border-top: 3px solid var(--accent-color);
        /* Adjust mobile padding/margin */
        padding-top: var(--space-xl);
        margin-top: var(--space-lg);
    }
    .coming-soon-badge {
        /* Adjust mobile position */
        top: calc(-1 * var(--space-md) + 2px); 
        left: var(--space-md);
        writing-mode: horizontal-tb;
    }
  }

  /* Define button styles locally */
  .btn {
    display: inline-block;
    padding: 0.9rem 2.2rem; 
    border-radius: var(--radius-default); 
    text-decoration: none;
    font-family: var(--font-sans);
    font-weight: 800; 
    font-size: 0.9rem;
    transition: all 0.2s ease-in-out;
    border: 1px solid transparent;
    cursor: pointer;
    text-align: center;
    letter-spacing: 0.5px;
    text-transform: uppercase; 
  }

  .btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 3px 6px rgba(0,0,0,0.1); 
  }

  .btn-primary {
    /* Default: Dark bg, light text, accent border */
    background-color: var(--card-bg-color);
    color: var(--primary-color);
    border-color: var(--accent-color);
  }

  .btn-primary:hover {
    /* Hover: Cyan bg, dark text */
    background-color: var(--accent-color); 
    border-color: var(--accent-color);
    color: var(--text-on-light);
  }
  
  .btn-secondary,
  .btn-outline {
    background-color: transparent;
    border-color: var(--primary-color); /* Light border */
    color: var(--primary-color); /* Light text */
  }
  
  .btn-secondary:hover,
  .btn-outline:hover {
    /* Corrected Hover: Transparent BG, Accent border/text */
    background-color: transparent;
    color: var(--accent-color);
    border-color: var(--accent-color);
  }

  /* Add style for intro paragraph */
  .section-intro {
      font-size: 1.1rem;
      color: var(--secondary-color);
    text-align: center;
    max-width: 800px;
      margin: 0 auto var(--space-2xl) auto;
  }

  /* Remove Placeholder Styles */
  /*
  .service-image { ... }
  .image-placeholder { ... }
  .image-placeholder::before { ... }
  */

</style> 